{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd meridian && npm install && npm run build",
  "outputDirectory": "meridian/dist",
  "installCommand": "echo \"skip root install\"",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
